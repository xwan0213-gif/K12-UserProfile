[project]
name = "k12-userprofile-api"
version = "0.1.0"
description = "K12 UserProfile FastAPI monolith scaffold"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
