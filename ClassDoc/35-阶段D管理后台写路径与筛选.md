# 阶段 D.4～D.6：管理后台写路径与筛选下钻 — 实现记录

**项目**：擎天学智 · K12 用户画像推荐系统  
**文档编号**：ClassDoc/35  
**日期**：2026-08-13  
**依据**：ClassDoc/30 阶段 D.4～D.6；ClassDoc/34 起步  
**操作人**：AI Agent（Cursor）

---

## 0. 范围与状态

| 项 | 状态 |
|----|------|
| D.4 组织 CRUD；员工创建/改/开账号 | ✅ |
| D.5 标签 POST/PATCH；话术 PATCH | ✅ |
| D.6 列表筛选 + 分页；AI 按日/区间/组织 | ✅ |
| D.7 空态/错误 + 本文档 | ✅（接续 34） |

**不做本迭代**：订单创建 UI；客户 PATCH / 客服摘要写 UI；阶段 E 企微/真实 ASR。

---

## 1. 前端改动摘要

| 页面 | 能力 |
|------|------|
| `OrgsView` | 新建 / 改 / 软删；仅 admin 可写；regional 只读 |
| `UsersView` | 筛选（关键词/角色/组织）+ 分页；新建/改；admin 开账号 |
| `TagsView` | 新建；行内 PATCH（名/SOP/启用）；顾问只读 |
| `ScriptsView` | 场景/启用筛选；新建；行内 PATCH；停用 DELETE |
| `CustomersView` | keyword + grade + PaginationBar |
| `OrdersView` | status + PaginationBar |
| `AiView` | `group_by` advisor/day；`from`/`to`；admin 可选 `org_id` |
| `nav.ts` / router | `/orgs` 菜单与路由；写权限 helper |

组件：`PaginationBar.vue`。

---

## 2. 后端微调

| 接口 | 说明 |
|------|------|
| `PATCH /admin/orgs/{id}` | 改为 `exclude_unset` 更新，支持显式 `parent_id: null` 清空上级 |

其余写接口（orgs/users/account/tags/scripts、customers/orders 分页筛选、ai/adoption）本阶段前端已对接，此前已存在。

---

## 3. 权限对齐

| 能力 | admin | regional | advisor |
|------|-------|----------|---------|
| 组织写/删 | ✓ | 查看 | — |
| 员工写 | ✓ | ✓（不可建 admin） | — |
| 开账号 | ✓ | — | — |
| 标签/话术写 | ✓ | ✓ | 查看 |
| AI 分析 | ✓ | ✓（本组织） | — |
| 客户/订单列表 | ✓ | ✓ | ✓（scope） |

---

## 4. 验证

```powershell
cd apps\admin; npm run build
# 开发：npm run dev → http://127.0.0.1:5174/admin/
```

| # | 步骤 | 期望 |
|---|------|------|
| 1 | admin：组织页创建组织 | 列表刷新；可改上级/编码；可软删 |
| 2 | admin：员工新建 + 开账号 | 可用新账号登录 |
| 3 | regional：员工页 | 可建顾问；无「开账号」；组织只读 |
| 4 | admin：标签新建并改 SOP | 侧栏词表可见 |
| 5 | 话术行内改标题后保存 | 列表更新 |
| 6 | 客户/订单翻页与筛选 | total 与翻页正确 |
| 7 | AI：按日 + 日期区间 | 行含 day；空区间可查 |

---

## 修订

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-08-13 | D.4～D.6 交付 |
