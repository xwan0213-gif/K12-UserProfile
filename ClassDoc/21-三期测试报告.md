# 三期测试报告

**日期**：2026-08-12  
**脚本**：`scripts/p3_smoke_test.py`（后端）  
**原始**：`ClassDoc/21-三期测试报告.json`（若有历史跑分可对照）  
**范围**：阶段 6 — 客服话术 + ASR + 日程/提醒 + 侧边栏 6.C  

---

## 1. 结论

| 维度 | 结果 |
|------|------|
| 后端冒烟 | **16/16 PASS**（2026-08-12 重构后复跑确认） |
| 日历同步 | 按设计降级 `sync_state=failed`，站内待办保留 — **不记失败** |
| 侧边栏 6.C | 日程 Tab / 客服切换 / 转写展示 / 弱提示条已接通（手工演示） |

---

## 2. 后端用例

| ID | 用例 | 期望 | 结果 |
|----|------|------|------|
| T-R-04a | ASR 转写写入 `asr_text` | 返回转写文本 / message_id | PASS |
| T-R-04 | 转写后仍可出建议；失败有提示 | 不阻断 suggest | PASS |
| T-R-05 | 客服 `scene=cs` 话术侧重服务 | 与销售话术可区分 | PASS |
| T-R-05b | `scene=service` 别名 → cs | 归一为 cs | PASS |
| T-S-01 | 时间语句生成日程草稿 | drafts 非空 | PASS |
| T-S-02 | 确认后站内可见；日历失败仍保留 | item 存在且可 `failed` | PASS |
| T-S-03 | 弱提醒 SSE；强提醒降级不崩 | weak delivered / strong degraded | PASS |
| T-S-04 | 提醒偏好读写 | pref PATCH/GET | PASS |

---

## 3. 前端手工验收（6.C）

| ID | 步骤 | 期望 | 结果 |
|----|------|------|------|
| UI-S-01 | 打开侧边栏「日程」Tab | 今日/本周筛选、AI 草稿、偏好 | 已实现待联调勾选 |
| UI-S-02 | AI 识别待办 → 确认 / 确认并同步日历 | 列表出现；同步失败有文案 | 已实现待联调勾选 |
| UI-S-03 | 待办点「弱」 | 顶栏 `WeakTipBar` 出现 | 已实现待联调勾选 |
| UI-R-01 | 建议 Tab 切「客服」→ 生成 | 客服话术；可反馈 | 已实现待联调勾选 |
| UI-R-02 | Mock 语音转写 → 建议区「基于转写」 | 展示转写摘要 | 已实现待联调勾选 |

---

## 4. 复跑命令

```powershell
docker compose -f deploy/docker-compose.yml up -d --build api nginx
python scripts\p3_smoke_test.py
cd apps\sidebar; npm run build
```

---

## 修订

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-08-11 | 后端 16/16 |
| V1.1 | 2026-08-12 | 增补 6.C 前端验收项；与实现记录 V1.1 对齐 |
