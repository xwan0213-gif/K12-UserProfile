# 阶段 C：侧栏壳与体验收尾 — 实现记录

**项目**：擎天学智 · K12 用户画像推荐系统  
**文档编号**：ClassDoc/33  
**日期**：2026-08-13  
**依据**：ClassDoc/30 阶段 C（P1/P2）  
**操作人**：AI Agent（Cursor）

---

## 0. 目标与完成判据

| 项 | 状态 |
|----|------|
| C.1 统一 Toast / 空态 / 按钮 loading | ✅ |
| C.2 弱提示点击跳转日程；能力条对齐 health | ✅ |
| C.3 演示控件收入「演示模式」折叠区 | ✅ |
| C.4 画像 timeline 结构化编辑；状态中文 | ✅ |
| C.5 标签推荐名不在词表时提示并跳过 | ✅ |
| 文档 + `npm run build` | ✅ |

---

## 1. 前端

| 文件 | 说明 |
|------|------|
| `composables/useToast.ts` | 全局 Toast 队列 |
| `components/shell/AppToast.vue` | 右下角 Toast |
| `components/shell/EmptyState.vue` | 统一空态 |
| `App.vue` | notify、演示模式、弱提示跳转、能力条数据 |
| `WeakTipBar.vue` | 点击文案 → 打开日程 |
| `CapabilityBar.vue` | 日历「降级 / 可同步」 |
| `profile/ProfilePanel.vue` | 时间线日期+事件编辑；状态中文 |
| `tags/TagsPanel.vue` | 不在词表警示 + 确认时跳过 |
| `styles/tokens.css` | `button.is-loading` |

---

## 2. 后端

| 接口 | 说明 |
|------|------|
| `GET /health` | 增补 `asr_provider`、`calendar_mode`、`calendar_hint` |

文件：`backend/app/main.py`

---

## 3. 顾问可读交互

| 操作 | 实际发生 |
|------|----------|
| 弱提醒条点击 | 切到日程 Tab 并关闭条 |
| 能力条「日历」 | 读 health，非写死 |
| 演示模式 | 物理场景 / 重新换票 / SSE 日志折叠 |
| 画像时间线修改 | 表单行，不写裸 JSON |
| 标签推荐不在词表 | 黄标提示；确认跳过并说明 |

---

## 4. 验证

```powershell
cd apps\sidebar; npm run build
# 可选：重建 api 使 health 新字段生效
docker compose -f deploy/docker-compose.yml up -d --build api
```

### 冒烟清单

| # | 步骤 | 期望 |
|---|------|------|
| 1 | 任意成功操作 | 右下角 Toast + 顶栏状态 |
| 2 | 触发弱提醒后点文案 | 进入日程 Tab |
| 3 | 看能力条日历 | Mock 下为「降级」 |
| 4 | 展开演示模式 | 见物理场景 / 换票 / SSE |
| 5 | 画像时间线修改 | 日期+事件行可增删 |
| 6 | AI 推荐不在词表名 | 黄标；确认提示跳过 |

---

## 修订

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-08-13 | 阶段 C 交付 |
