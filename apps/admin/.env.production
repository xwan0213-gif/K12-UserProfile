# Production / Docker nginx (same origin). Leave empty so redirects use relative / and /admin/.
# Cross-port Vite dev still uses apps/*/.env.development with 5173/5174.
VITE_SIDEBAR_URL=
VITE_ADMIN_URL=
